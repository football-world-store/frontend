import type { DashboardPeriod } from "@/types";

export const DEFAULT_DASHBOARD_PERIOD: DashboardPeriod = {
  period: "LAST_30_DAYS",
};

export const DEFAULT_DASHBOARD_TOP_LIMIT = 10;
export const DEFAULT_IDLE_DAYS = 30;
